# Backward-compatible full install.
#
# This intentionally includes the same runtime families as the historical
# single requirements file. Prefer installing the profile files directly when
# you want a smaller or more explicit environment.

-r requirements-core.txt
-r requirements-optional.txt
-r requirements-wav2lip.txt
-r requirements-gpu-cu124.txt
